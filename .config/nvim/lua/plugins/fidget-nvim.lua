require('fidget').setup({
    notification = {
        override_vim_notify = true,
        view = {
            stack_upwards = false,
        },
        window = {
            border = 'rounded',
        },
    },
})
